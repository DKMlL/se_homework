# name指的是这套工作流程的名称，可以自定义
name: CI

# on是指这套工作流程的trigger条件，即在push时触发
on: [push]


jobs:

  build:

    
    runs-on: ubuntu-latest
    
   
    steps:
    
    - uses: actions/checkout@v1
   
    - name: executing
      run: 
       
        cat TestAction.java
